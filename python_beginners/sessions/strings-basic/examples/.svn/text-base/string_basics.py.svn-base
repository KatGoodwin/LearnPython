#!/usr/bin/env python

simple = "I am a string"
print simple

print "I'm a simple test"

# len() returns the length of a string - or any sequence
print "The string is", len(simple), "characters long"
