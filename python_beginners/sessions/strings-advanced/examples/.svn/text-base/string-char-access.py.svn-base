# strings are an immutable character list, we can access them using
# array-like syntax [ ]

str = '0123456789'
print "The fifth character is : " + str[ 4 ]
print "The second to last character is : " + str[-2]

# This fails! (strings are immutable)
str[5] = "A"
